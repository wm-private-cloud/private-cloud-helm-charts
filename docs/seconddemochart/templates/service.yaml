apiVersion: v1
kind: Service
metadata:
  name: {{ include "seconddemochart.fullname" . }}
  labels:
    {{- include "seconddemochart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seconddemochart.selectorLabels" . | nindent 4 }}
