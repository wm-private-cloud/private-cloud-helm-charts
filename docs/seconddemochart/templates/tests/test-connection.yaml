apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seconddemochart.fullname" . }}-test-connection"
  labels:
    {{- include "seconddemochart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seconddemochart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
